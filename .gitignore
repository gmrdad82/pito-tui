# Binaries
/pito-tui
/dist/

# Test artifacts
cover.out
*.test

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store

# Claude guardrails ship with the repo (pito model): hooks + settings.json
# are committed; everything else in .claude/ (local settings, runtime state)
# stays machine-local.
.claude/*
!.claude/hooks/
!.claude/settings.json

# Local-only agent/working docs — never checked in.
/.claude/INBOX.md

# Capture artifacts (scripts/capture.sh)
/tmp/
